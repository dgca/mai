---
name: assume-persona--session-restore
description: |
  Restores previously loaded personas when resuming a session. Invoke when: resuming session, continuing session, picking up where we left off, back to work, starting again, session resume, restoring context, reload personas, restore personas, what personas were loaded, which personas active, where were we, let's continue.
user-invocable: false
---

Use the `persona_restore` tool to restore any previously loaded personas from the session state.

This will reload the expertise context from the prior session, re-injecting the persona content.
